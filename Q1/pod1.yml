apiVersion: v1

kind: pod
metadata:
        name: pod1
spec:
        containers:
                - image: httpd:2.4.41-alpine
                  name: pod1-container
                  ports:
                          - containerPort: 80
